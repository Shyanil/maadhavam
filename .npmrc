; React 19 is compatible at runtime with react-helmet-async, but that package's
; peer range only lists up to React 18, so strict npm installs (e.g. Netlify)
; fail with ERESOLVE. Allow the install to proceed.
legacy-peer-deps=true
